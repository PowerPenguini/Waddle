app-id: io.github.powerpenguini.Waddle
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.rust-stable
command: waddle

finish-args:
  - --device=dri
  - --share=ipc
  - --socket=wayland
  - --socket=fallback-x11
  - --filesystem=host
  - --talk-name=org.gtk.vfs.*
  - --system-talk-name=org.freedesktop.UDisks2

modules:
  - name: waddle
    buildsystem: simple
    build-options:
      append-path: /usr/lib/sdk/rust-stable/bin
      env:
        CARGO_HOME: /run/build/waddle/cargo
        CARGO_NET_OFFLINE: 'true'
    build-commands:
      - cargo build --offline --locked --release
      - install -Dm0755 target/release/waddle /app/bin/waddle
      - install -Dm0644 data/io.github.powerpenguini.Waddle.desktop /app/share/applications/io.github.powerpenguini.Waddle.desktop
      - install -Dm0644 data/io.github.powerpenguini.Waddle.metainfo.xml /app/share/metainfo/io.github.powerpenguini.Waddle.metainfo.xml
      - install -Dm0644 data/icons/hicolor/scalable/apps/io.github.powerpenguini.Waddle.svg /app/share/icons/hicolor/scalable/apps/io.github.powerpenguini.Waddle.svg
    sources:
      - type: dir
        path: ..
        skip:
          - .git
          - .flatpak-builder
          - build-flatpak
          - dist
          - target
      - cargo-sources.json
